<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Cyber Book Nook</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Inline CSS to Remove White Box and Adjust Layout -->
    <style>
      body, html {
        margin: 0;
        padding: 0;
        height: 100%;
      }

      .header, .footer {
        background-color: #990000;
        color: white;
        text-align: center;
        padding: 30px 0;
        width: 100%;
      }

      .header h1, .footer p {
        margin: 0;
      }

      .content {
        max-width: 1100px;
        margin: 40px auto;
        padding: 30px;
        background-color: white;
        border: 1px solid #ddd;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin: 30px 0;
        border: 1px solid #ddd;
      }

      th, td {
        padding: 20px;
        border: 1px solid #ccc;
        text-align: center;
      }

      th {
        background-color: #990000;
        color: white;
      }

      .btn {
        display: inline-block;
        padding: 10px 20px;
        text-decoration: none;
        border-radius: 6px;
        font-size: 1em;
        margin: 5px;
      }

      .btn-primary {
        background-color: #990000;
        color: white;
      }

      .btn-danger {
        background-color: #660000;
        color: white;
      }

      .btn-warning {
        background-color: #ffcc00;
        color: black;
      }

      .action-buttons {
        display: flex;
        justify-content: center;
        gap: 15px;
      }

      .alert, .notice {
        padding: 15px;
        margin: 15px 0;
        border-radius: 5px;
        text-align: center;
      }

      .alert {
        background-color: #f8d7da;
        color: #721c24;
      }

      .notice {
        background-color: #d4edda;
        color: #155724;
      }
    </style>

    <!-- JavaScript Imports -->
    <%= javascript_importmap_tags %>
    <script type="module">
      import "application";
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" defer></script>
    <%= turbo_include_tags %>
  </head>

  <body>
    <header class="header">
      <h1>Cyber Book Nook</h1>
      <p>Your portal to classic cyberpunk literature</p>
    </header>

    <main class="content">
      <% if flash[:notice] %>
        <div class="notice"><%= flash[:notice] %></div>
      <% end %>

      <% if flash[:alert] %>
        <div class="alert"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="footer">
      <p>&copy; 2024 Cyber Book Nook | All rights reserved.</p>
    </footer>
  </body>
</html>
